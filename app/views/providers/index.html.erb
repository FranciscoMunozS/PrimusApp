<p id="notice"><%= notice %></p>

<div class="row">
  <div class="col-md-6"><h1>Proveedores</h1></div>
  <div class="col-md-6 text-right"><%= link_to 'Nuevo proveedor', new_provider_path, class: 'btn btn-primary' %></div>
</div>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Nombre proveedor</th>
      <th>RUT proveedor</th>
      <th>Descripción</th>
      <th>Correo electronico</th>
      <th>Telefono</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @providers.each do |provider| %>
      <tr>
        <td><%= provider.name %></td>
        <td><%= provider.rut %></td>
        <td><%= provider.description %></td>
        <td><%= provider.email %></td>
        <td><%= provider.phone %></td>
        <td><%= link_to 'Ver', provider %></td>
        <td><%= link_to 'Editar', edit_provider_path(provider) %></td>
        <td><%= link_to 'Eliminar', provider, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
